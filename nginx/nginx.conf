# Nginx reverse proxy configuration for ChemVault production deployment

# Rate limiting zones
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=general_limit:10m rate=30r/s;

# Connection limiting (prevent resource exhaustion attacks)
limit_conn_zone $binary_remote_addr zone=conn_limit:10m;

# Upstream backend servers
upstream backend {
    server backend:8000 max_fails=3 fail_timeout=30s;
    keepalive 32;
}

# HTTP server - redirect to HTTPS in production
server {
    listen 80;
    server_name localhost;

    # Connection limit per IP
    limit_conn conn_limit 20;

    # Allow health checks on HTTP
    location = /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }

    # Redirect all other traffic to HTTPS (uncomment for production)
    # location / {
    #     return 301 https://$host$request_uri;
    # }

    # For development without SSL, include the main configuration here
    include /etc/nginx/conf.d/locations.conf;
}

# HTTPS server - uncomment and configure for production with SSL
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#
#     # Connection limit per IP
#     limit_conn conn_limit 20;
#
#     # SSL certificates (use Let's Encrypt or your certificates)
#     ssl_certificate /etc/nginx/ssl/cert.pem;
#     ssl_certificate_key /etc/nginx/ssl/key.pem;
#
#     # SSL configuration
#     include /etc/nginx/conf.d/ssl-params.conf;
#
#     # Main configuration
#     include /etc/nginx/conf.d/locations.conf;
# }

# Optional: Server block for redirecting www to non-www
# server {
#     listen 443 ssl http2;
#     server_name www.your-domain.com;
#
#     ssl_certificate /etc/nginx/ssl/cert.pem;
#     ssl_certificate_key /etc/nginx/ssl/key.pem;
#
#     return 301 https://your-domain.com$request_uri;
# }
